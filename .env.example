# GitHub personal access token used to fetch the contribution graph.
# A classic token with the "read:user" scope is enough (contribution data is public).
# Create one at https://github.com/settings/tokens and copy it into .env.local
GITHUB_TOKEN=
